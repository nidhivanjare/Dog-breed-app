<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dog_breed">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GS_Pit_Bull"></activity>
        <activity android:name=".Goldendoodle" />
        <activity android:name=".Husky" />
        <activity android:name=".German_Spitz" />
        <activity android:name=".bull_dog" />
        <activity android:name=".Golden_Retriever" />
        <activity android:name=".Home_page" />
        <activity android:name=".Sign_up" />
        <activity android:name=".Sign_in" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>